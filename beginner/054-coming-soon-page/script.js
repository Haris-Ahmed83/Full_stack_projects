// Project #54: Coming Soon Page
// Key Concepts: Countdown Timer, CSS Animations (implied triggers), Email Signup, Social Links

document.addEventListener('DOMContentLoaded', () => {

    // --- Countdown Timer ---
    const countdownElement = document.getElementById('countdown');
    const daysSpan = document.getElementById('days');
    const hoursSpan = document.getElementById('
